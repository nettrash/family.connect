# Robolectric (<= 4.15.1) supports at most SDK 35, but the app targets SDK 36,
# so without a pin every test fails at runner init with
# "Package targetSdkVersion=36 > maxSdkVersion=35".
# Drop this once a Robolectric release supports SDK 36.
# (Same pin as Scan.Android.)
sdk=35
