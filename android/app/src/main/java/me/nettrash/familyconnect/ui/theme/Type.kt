/*
 * Type.kt
 * Family Connect (Android)
 *
 * Typography — Material 3 defaults. A chat app lives and dies on system
 * legibility; no custom font families, no downloadable fonts.
 *
 * iOS counterpart: ios/FamilyConnect/UI/Theme/Typography.swift
 */

package me.nettrash.familyconnect.ui.theme

import androidx.compose.material3.Typography

val FamilyConnectTypography = Typography()
