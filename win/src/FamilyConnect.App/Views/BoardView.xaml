<UserControl
    x:Class="FamilyConnect.App.Views.BoardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Structure and x:Name only: every word is set from code, through the catalogue. -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Padding="24,0,24,8" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="Heading" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock x:Name="StatusText" TextWrapping="Wrap" Visibility="Collapsed" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                <Button x:Name="AddNoteButton" />
                <Button x:Name="AddEventButton" />
                <Button x:Name="AddListButton" />
                <Button x:Name="PinPhotoButton" />
            </StackPanel>
            <Button x:Name="DoneButton" Grid.Column="2" VerticalAlignment="Center" Style="{StaticResource AccentButtonStyle}" />
        </Grid>
        <ScrollViewer x:Name="Scroller" Grid.Row="1" AllowDrop="True" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <Canvas x:Name="Wall" />
        </ScrollViewer>
        <StackPanel x:Name="Empty" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="2" IsHitTestVisible="False" Visibility="Collapsed">
            <TextBlock x:Name="EmptyTitle" HorizontalAlignment="Center" FontWeight="SemiBold" />
            <TextBlock x:Name="EmptyText" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
