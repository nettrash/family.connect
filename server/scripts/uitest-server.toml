[server]
bind = "127.0.0.1:8091"

[database]
host = "127.0.0.1"
port = 5432
user = "postgres"
password = "postgres"
database = "family_connect_dev"

# Attachments need a writable directory or the server refuses to start;
# /tmp keeps the fixture disposable like the database above.
[storage]
attachments_dir = "/tmp/fc-uitest-attachments"
